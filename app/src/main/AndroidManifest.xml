<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.judoacademy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.JudoAcademy">
        <activity android:name=".AddNotesActivity"></activity>
        <activity android:name=".NotesActivity" />
        <activity android:name=".AttendanceDetailActivity" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".StudentDetailsActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>